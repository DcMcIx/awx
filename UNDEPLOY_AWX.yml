---
- name: DésInstallation AWX
  hosts: all
  become: yes
  gather_facts: yes
  tasks:

    - name: Include AWX Roles
      include_role: 
        name: awx_undeploy
      when: ansible_facts['os_family'] == "Debian"